include "root" {
  path   = find_in_parent_folders("root-config.hcl")
  expose = true
}

include "stage" {
  path   = find_in_parent_folders("stage.hcl")
  expose = true
}

locals {
  # merge tags
  local_tags = {
    "Name" = "SecurityGroups"
  }

  tags = merge(include.root.locals.root_tags, include.stage.locals.tags, local.local_tags)
}

generate "provider_global" {
  path      = "provider.tf"
  if_exists = "overwrite"
  contents  = <<EOF
terraform {
  backend "s3" {}
  required_version = "${include.root.locals.version_terraform}"
  required_providers {
    aws = {
      source = "hashicorp/aws"
      version = "${include.root.locals.version_provider_aws}"
    }
  }
}

provider "aws" {
  region = "${include.root.locals.region}"
}
EOF
}

inputs = {
  # Inputs for the security groups
  vpc_id     = "vpc-0ea0e7962096f70ce" # Replace with your VPC ID
  my_ip      = "223.235.186.115"   # Replace with your IP address
  
  # Additional inputs if needed
  tags = local.tags
}

terraform {
  source = "${get_parent_terragrunt_dir("root")}/../terraform/security-groups"
}
